# 改网站 · 直接找我就行

**结论：你不需要再执行任何命令、输入 token 或操作 GitHub 网页。**
直接在对话里告诉我改什么，我改完、发布完、验证完再回报给你。

---

## 我这边会自动完成的事

| 步骤 | 我做什么 |
| --- | --- |
| 1 | 修改文件（`index.html` / `assets/js/data.js` / `assets/css/style.css`） |
| 2 | 跑自检脚本，确认图片引用、CSS/JS 语法、页面交互都没问题 |
| 3 | `git commit` 提交到本地仓库 |
| 4 | `git push` 推送到 GitHub（凭据已保存在本机，不用你输密码） |
| 5 | GitHub Actions 自动部署（约 1 分钟） |
| 6 | 从线上抓取页面复查，确认改动真的生效了 |

第 4~6 步是**全自动**的，你只需要等我回报结果。

---

## 你只需要说一句话，例如

| 你说 | 我做什么 |
| --- | --- |
| “把邮箱改成 xxx@qq.com，电话改成 138xxxx” | 改 `index.html` 的联系区块并发布 |
| “首页标语改一下，太土了” | 改文案 + 首屏标题，发布 |
| “坐式砂光那个产品描述重写一下” | 改 `data.js` 里对应条目，发布 |
| “这几张图不好看，换掉”（发我图片文件夹名） | 重新处理图片 + 更新数据，发布 |
| “我这有新产品，放在 xxx 文件夹了” | 生成图片、加数据、加卡片，发布 |
| “手机上产品卡片太挤了” | 调 CSS 断点与间距，发布 |
| “标题字再大一点 / 金色再淡一点” | 调变量，发布 |
| “加个案例展示 / 加个视频区” | 加新板块，发布 |

---

## 关于「先给我看再发布」

如果你担心改完直接上线不合适，可以这样说：

- **“先改给我看，别发布”** → 我只改本地文件，跑 `node server.mjs` 让你在
  `http://127.0.0.1:5173` 预览，你满意了再说“发布”，我才推送
- **默认行为**：改动小的（错别字、联系方式）我直接发布；**改动大的**（改版、
  换配色、加板块）我会先把方案或预览给你确认

---

## 我做不到的三件事（这三件仍需你操作）

1. **创建 / 删除 GitHub 账号**，或改账号密码 —— 只有你能做
2. **绑定自定义域名**（如 `www.meilianmei.com`）—— 需要你去域名服务商添加解析记录，
   我可以在你买好域名后指导每一步
3. **需要你本人登录的操作**，例如查看你私密仓库的日志、改仓库可见性设置

除此之外，网站相关的所有事情都能交给我。

---

## 两个前提

1. **文件要留在这台电脑**：路径 `C:\Users\Yangwenhao\Desktop\网页制作`。
   如果你把文件夹改名或移动，告诉我新路径即可。
2. **加速器偶尔会抽风**：如果推送时连不上 GitHub（报 TLS handshake 或超时），
   我会自动重试几次；仍然失败我会告诉你“开一下加速器”，然后重试。

---

## 网址与状态自查

- 网站：https://wenhaoyang1.github.io/meilianmei/
- 仓库：https://github.com/wenhaoyang1/meilianmei
- 部署记录：https://github.com/wenhaoyang1/meilianmei/actions
- 改完约 **1 分钟**线上可见；如果没变化，等 1~5 分钟（CDN 传播），不是出错

---

## 万一你想自己做（备用，平时用不上）

```powershell
cd C:\Users\Yangwenhao\Desktop\网页制作
git add -A
git commit -m "更新内容"
git push
```

不用输密码（已保存）。另外桌面上有 `一键推送.ps1`，双击运行即可自动完成推送。
详细排查手册见 `部署到GitHub.md`。
